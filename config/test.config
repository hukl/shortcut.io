[
    {kernel, [
        {logger, [
            {handler, default, logger_std_h, #{config => #{type => {file,"log/test.log"}}}},
            {handler, info,    logger_std_h, #{level => debug, config => #{type => {file, "log/debug.log"}}}}
        ]}
    ]},

    {scio, [
        {http_port, 9090}
    ]}
].
