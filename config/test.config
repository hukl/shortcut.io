[
    {kernel, [
        {logger, [
            {handler, default, logger_std_h, #{config => #{
                type => {file,"log/test.log"},
                sync_mode_qlen => 0,
                drop_mode_qlen => 1000000,
                flush_qlen => 1000000,
                burst_limit_enable => false,
                overload_kill_enable => false
            }}}
        ]}
    ]},

    {scio, [
        {http_port, 9090}
    ]}
].
